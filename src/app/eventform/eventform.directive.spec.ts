import { EventformDirective } from './eventform.directive';

describe('EventformDirective', () => {
  it('should create an instance', () => {
    const directive = new EventformDirective();
    expect(directive).toBeTruthy();
  });
});
