import { SecondeventDirective } from './secondevent.directive';

describe('SecondeventDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondeventDirective();
    expect(directive).toBeTruthy();
  });
});
