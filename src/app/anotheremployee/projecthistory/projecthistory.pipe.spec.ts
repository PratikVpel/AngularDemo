import { ProjecthistoryPipe } from './projecthistory.pipe';

describe('ProjecthistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjecthistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
